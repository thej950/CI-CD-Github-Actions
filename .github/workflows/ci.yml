name: CI-pipeline 
on: push 
jobs: 
  build: 
    runs-on: ubuntu-latest 
    steps: 
      - name: print hello 
        run: echo "hello world"
      - name: show current time
        run: date
      - name: list files in workspace
        run: ls -la